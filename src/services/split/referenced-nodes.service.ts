import { FilePath } from './module.type';
import { openFile } from '../../vendors/helpers/file.helper';
import { parse } from '@babel/parser';

const listReferencedNodes = (filePath: FilePath) => {
  let result = 0;

  return result;
}

export { listReferencedNodes };
