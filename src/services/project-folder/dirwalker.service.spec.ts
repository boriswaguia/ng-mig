import { dirWalker } from './dirwalk.service';

describe('DirWalkerService', () => {
  it('should walker project dir and open files', () => {
    const projectDir = '';
    dirWalker(projectDir)
  });
});
